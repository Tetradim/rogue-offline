{
  "name": "pokerogue-pokemon-creator",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:ui": "vite --open /dev.html",
    "start": "node server/index.js --open",
    "build": "vite build",
    "postbuild": "node postbuild.js",
    "preview": "vite preview",
    "sync:enum-metadata": "node scripts/sync-enum-metadata.mjs",
    "check:service": "node --check server/index.js && node --check server/app.js && node --check server/windows-dialog.js && node --check server/target-discovery.js && node --check server/target-verifier.js && node --check server/target-preflight-service.js && node --check server/delivery-service.js",
    "check-installer": "node --check installer-core.cjs && node --check pokerogue-mod-installer.cjs && node --check pokerogue-mod-package-installer.cjs && node --check pokerogue-mod-target-preflight.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.6.3",
    "@testing-library/react": "^16.3.0",
    "@testing-library/user-event": "^14.6.1",
    "@vitejs/plugin-react": "^4.3.0",
    "@vitest/coverage-v8": "^3.2.4",
    "jsdom": "^26.1.0",
    "vite": "^6.0.0",
    "vitest": "^3.2.4"
  }
}
